interface:
  display_name: "Linear Batch Implement"
  short_description: "Batch orchestrate ready Linear implementation work."
  default_prompt: "Use $linear-batch-implement to list llm-ready issues with ready plans, confirm the queue and parallelism, run linear-implement per issue, and aggregate results."
dependencies:
  tools:
    - type: "mcp"
      value: "linear"
      description: "Linear MCP server for ready issue discovery, plan comment inspection, and stale-state re-read checks."
policy:
  allow_implicit_invocation: true
