interface:
  display_name: "Linear Refine"
  short_description: "Clarify Linear issues into ready plans."
  default_prompt: "Use $linear-refine to clarify this Linear issue into an implementation-ready plan."
dependencies:
  tools:
    - type: "mcp"
      value: "linear"
      description: "Linear MCP server for issue reads, comments, labels, and status updates."
policy:
  allow_implicit_invocation: true
